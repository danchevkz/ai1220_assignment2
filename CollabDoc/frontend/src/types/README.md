# `src/types/`

Shared TypeScript types. Request/response shapes that are used in more than
one place (auth tokens, `Document`, `Collaborator`, `ShareLink`, AI
interaction history items) live here so API helpers, stores, and components
all agree on the same definitions.
